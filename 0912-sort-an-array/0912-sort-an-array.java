class Solution {
    public int[] sortArray(int[] nums) {
        // ArrayList<String> arr=new ArrayList<>();
        // for(int val:nums){
        //     arr.add(String.toString(val));
        // }
        // Collections.sort(arr);
        // int[] num=new int[nums.length];
        // for(String val:nums.toArray()){
        //     arr.add(String.toString(val));
        // }
        Arrays.sort(nums);
        return nums;
    }
}